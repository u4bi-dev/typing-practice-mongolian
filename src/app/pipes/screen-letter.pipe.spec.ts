import { ScreenLetterPipe } from './screen-letter.pipe';

describe('ScreenLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreenLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
